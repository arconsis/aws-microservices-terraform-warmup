const usersDataStore = require('./users/usersDataStore');

module.exports = (db) => {
  const obj = Object.assign(
    {},
    db.entities,
    {
      sequelize: db.sequelize,
    },
  );
  return {
    usersDataStore: usersDataStore(obj),
  };
};
