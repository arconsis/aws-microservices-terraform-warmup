const postsDataStore = require('postsDatabase/dataStores/posts/postsDataStore');

module.exports = (db) => {
  const obj = Object.assign(
    {},
    db.entities,
    {
      sequelize: db.sequelize,
    },
  );
  return {
    postsDataStore: postsDataStore(obj),
  };
};
