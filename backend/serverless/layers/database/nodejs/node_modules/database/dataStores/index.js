const adminsDataStore = require('database/dataStores/admins/adminsDataStore');
const usersDataStore = require('database/dataStores/users/usersDataStore');

module.exports = (db) => {
  const obj = Object.assign(
    {},
    db.entities,
    {
      sequelize: db.sequelize,
    },
  );
  return {
    adminsDataStore: adminsDataStore(obj),
    usersDataStore: usersDataStore(obj),
  };
};
